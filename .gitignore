node_modules/
*.env
.env
backend/.env
/backend/server
/backend/bin/
*.exe
tmp/
agent/motech-connect.exe

# Code-signing private key (NEVER commit). Public cert + pfx handled per-policy.
agent/signing/*.key
agent/signing/cert.pfx
agent/signing/ca.key
